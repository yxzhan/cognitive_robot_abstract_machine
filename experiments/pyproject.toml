[project]
name = "experiments"
version = "0.0.1"
description = "Experiment scripts and actions for CRAM."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true
